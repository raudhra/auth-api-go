# Go Authentication REST API (JWT)

A simple, production-style authentication REST API built in **Go**, featuring **JWT-based authentication**, **password hashing**, and **PostgreSQL** integration.

This project demonstrates how real backend systems handle:
- User registration
- Secure login
- Token-based authentication
- Protected routes

---

## 🚀 Features

- User Registration (`/register`)
- User Login with JWT (`/login`)
- Protected Endpoint (`/profile`)
- Password hashing using **bcrypt**
- JWT authentication middleware
- PostgreSQL database
- Environment-based configuration

---

## 🛠 Tech Stack

- **Go (Golang)**
- **net/http** – HTTP server
- **chi** – Router & middleware
- **PostgreSQL** – Database
- **bcrypt** – Password hashing
- **JWT** – Authentication tokens
- **godotenv** – Environment variables

---

## 📁 Project Structure

